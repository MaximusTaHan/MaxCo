@model MaxCoViewModels

@{
    ViewData["Title"] = "Home Page";
}

<div class="main-container container">
    @{
        @Html.Partial("_FeatureProduct")
    }
    <div class="row">
        <div class="col-md-4 col-ms-4 d-flex justify-content-center categoryButtons">
            <form asp-controller="Home" asp-action="Category" class="w-100">
                <input type="submit" name="categorySearch" value="Nintendo" class="categoryItem w-100" />
            </form>
        </div>
        <div class="col-md-4 col-ms-4 d-flex justify-content-center categoryButtons">
            <form asp-controller="Home" asp-action="Category" class="w-100">
                <input type="submit" name="categorySearch" value="Sony" class="categoryItem w-100" />
            </form>
        </div>
        <div class="col-md-4 col-ms-4 d-flex justify-content-center categoryButtons">
            <form asp-controller="Home" asp-action="Category" class="w-100">
                <input type="submit" name="categorySearch" value="Microsoft" class="categoryItem w-100" />
            </form>
        </div>
    </div>
    <div class="row">
        <div class="col-md-4 col-sm-4  d-flex justify-content-center categoryButtons">
            <form asp-controller="Home" asp-action="Browse" class="w-100">
                <input type="submit" value="Browse" class="categoryItem w-100"/>
            </form>
        </div>
        <div class="col-md-4 col-sm-4  d-flex justify-content-center categoryButtons">
            <form asp-controller="Home" asp-action="Category" class="w-100">
                <input type="submit" name="categorySearch" value="Games" class="categoryItem w-100" />
            </form>
        </div>
        <div class="col-md-4 col-sm-4  d-flex justify-content-center categoryButtons">
            <form asp-controller="Home" asp-action="Category" class="w-100">
                <input type="submit" name="categorySearch" value="Controller" class="categoryItem w-100" />
            </form>
        </div>
    </div>
</div>